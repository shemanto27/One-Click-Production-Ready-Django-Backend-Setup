SECRET_KEY=django-insecure-{{ secret_key }}
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,{{ website_domain }}
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://{{ website_domain }}

# Database (PostgreSQL recommended for production)
DB_ENGINE=django.db.backends.postgresql
DB_NAME={{ project_name }}db
DB_USER=test-user
DB_PASSWORD=test-password
DB_HOST=localhost
DB_PORT=5432
DATABASE_URL=postgres://test-user:test-password@localhost:5432/{{ project_name }}db

# AWS Settings (Optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1

# Sentry (Optional)
SENTRY_DSN=

# Email Settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=
