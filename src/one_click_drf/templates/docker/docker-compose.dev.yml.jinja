version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: {{ project_name }}-backend
    container_name: {{ project_name }}-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
