version: "3.9"

services:
  backend:
    image: {{ dockerhub_username }}/{{ project_name }}:backend-latest
    container_name: {{ project_name }}-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
